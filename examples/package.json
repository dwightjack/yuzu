{
  "name": "examples-yuzu",
  "version": "2.0.1",
  "main": "dist/index.ts",
  "author": "Marco Solazzi",
  "license": "MIT",
  "private": true,
  "dependencies": {
    "redux": "^4.0.4",
    "yuzu": "^2.0.1",
    "yuzu-application": "^2.0.1",
    "yuzu-loadable": "^2.0.1",
    "yuzu-polyfills": "^2.0.1",
    "yuzu-utils": "^2.0.1"
  },
  "scripts": {
    "build": "tsdx build --format umd",
    "start:tsdx": "tsdx watch --format umd",
    "start:server": "browser-sync start -s --f dist/** index.html --w --ss ../packages",
    "start": "run-p start:*"
  },
  "devDependencies": {
    "browser-sync": "^2.26.7",
    "npm-run-all": "^4.1.5"
  }
}
